# Railway configuration for FlareWeather Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port 8000"
restartPolicyType = "on_failure"

[env]
PYTHON_VERSION = "3.11"
PORT = "8000"
