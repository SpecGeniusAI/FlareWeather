# Railway configuration for FlareWeather Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn test_main:app --host 0.0.0.0 --port 8080"
restartPolicyType = "on_failure"

[env]
PYTHON_VERSION = "3.11"
PORT = "8000"
