# Production environment variables
# Copy this to your hosting platform's environment variables

# OpenAI API Key (required for AI insights)
OPENAI_API_KEY=your_openai_api_key_here

# Server configuration
HOST=0.0.0.0
PORT=8000

# Optional: Enable debug mode (set to false in production)
DEBUG=false

# Optional: CORS origins (comma-separated)
CORS_ORIGINS=https://your-ios-app-domain.com,http://localhost:3000
